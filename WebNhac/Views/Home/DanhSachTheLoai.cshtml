@using WebNhac.Models;
@model List<tbTheLoai>


<div class="recommended-grids english-grid" style="padding-left: 31px; padding-right: 20px">
    <div class="recommended-info">
        <div class="heading">
            <h3 style="float: left">Danh sách chủ đề</h3>
        </div>
        <div class="clearfix"> </div>
    </div>
    @foreach (var item in Model)
    {
        <div class="col-md-3 resent-grid recommended-grid show-video-grid" style="margin-top: 1%">
            <div class="resent-grid-img recommended-grid-info">
                <a href="@Url.Action("BaiHatTheoChuDe", "BaiHat", new { id = item.MaTheLoai})"><img src="@Url.Content("~/Content/images/ChuDe/" + item.AnhTheloai)" alt="" style="width: 100%; height: 150px" /></a>
            </div>
            <div class="resent-grid-info recommended-grid-info" style="margin-top: 1%">
                <p class="author" style="width: 100%; height: 50px"><a href="@Url.Action("BaiHatTheoChuDe", "BaiHat", new { id = item.MaTheLoai})" class="author">@item.TenTheLoai</a></p>
                <p class="author"><a href="@Url.Action("BaiHatTheoChuDe", "BaiHat", new { id = item.MaTheLoai})" class="author">Số bài hát: @item.tbNhacs.Count()</a></p>
            </div>
        </div>
    }
    <div class="clearfix"> </div>
    <div style="margin-top: 20px">
        <center><a href="@Url.Action("TatCaChuDe", "ChuDe")" class="btn btn-primary">Xem tất cả</a></center>
    </div>
</div>



